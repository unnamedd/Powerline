dependencies:
  override:
    - eval "$(curl -sL https://raw.githubusercontent.com/Zewo/Scripts/master/install.sh)"
test:
  override:
    - eval "$(curl -sL https://raw.githubusercontent.com/Zewo/Scripts/master/build.sh)"
