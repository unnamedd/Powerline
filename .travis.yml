os:
  - linux
  - osx
language: generic
sudo: false
dist: trusty
osx_image: xcode8.3
install:
  - eval "$(curl -sL https://raw.githubusercontent.com/Zewo/Scripts/master/install.sh)"
script:
  - bash <(curl -s https://raw.githubusercontent.com/Zewo/Scripts/master/build.sh) Powerline